# run in crontab as
# * * * * * /usr/sbin/logrotate ~meddler/apps/logrotate.conf -s /tmp/logrotate.tmp
/home/meddler/apps/node.iterator.micro/server.js.out.txt {
        size 1M
        # size 1k
        copytruncate
        # create 700 meddler meddler
        # dateext
        rotate 5
        # compress
}